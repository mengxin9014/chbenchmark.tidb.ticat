. "`cd $(dirname ${BASH_SOURCE[0]}) && pwd`/ticat.helper.bash/helper.bash"
. "`cd $(dirname ${BASH_SOURCE[0]}) && pwd`/tiup.helper.bash/tiup.bash"
. "`cd $(dirname ${BASH_SOURCE[0]}) && pwd`/bench-toolset.helper.bash/bench-toolset.bash"

. "`cd $(dirname ${BASH_SOURCE[0]}) && pwd`/mysql.bash"
. "`cd $(dirname ${BASH_SOURCE[0]}) && pwd`/tiup.bash"
